version: "3"

services:
    redis:
        restart: always
    backend:
        image: ${BACKEND_IMAGE}
        restart: always
        volumes:
            - /backend
            - /midi_files
        environment:
            - ENV=prod
    frontend:
        image: ${FRONTEND_IMAGE}
        restart: always
        ports:
            - "80:80"
